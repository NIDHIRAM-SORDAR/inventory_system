# inventory_system/constants.py
import os

# Default profile picture path
DEFAULT_PROFILE_PICTURE = "/icons/profile.png"
LOG_DIR = LOG_DIR = os.path.join(os.getcwd(), "assets", "logs", "audit.log")  # Logs will be stored in the "assets/logs" directory within the workspace